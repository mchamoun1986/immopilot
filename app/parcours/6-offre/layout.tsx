import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Faire une offre d'achat — Étape 6 | ImmoPilot",
  description: "Comment rédiger une offre d'achat, négocier le prix et sécuriser votre proposition immobilière.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
